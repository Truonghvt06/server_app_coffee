{
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "json-server --host 192.168.0.102 --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
